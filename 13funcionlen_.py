frase = """
Esta es una cadena muy larga de tres caracteres 
de comillas que son utilizados para definir cadenas multilineas
"""
first_half = int(len(frase)/2) #funcion len devuelve el numero de caracteres que tiene un arreglo
print(frase[:first_half])